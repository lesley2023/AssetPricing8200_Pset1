Provide me with Python code following these steps:
1. Read the csv file "EQ_Dataset.csv" into a pandas DataFrame
2. Display the first few rows of the DataFrame to get an overview of the data.
3. Create a monthly date variable using "YEAR" and "MONTH", and sort the DataFrame chronologically.
4. Check for any missing values in the DataFrame to ensure data completeness.
5. Create the variable 'bar_dp', and it is the mean of the "dp".
6. Create the variable 'kappa' as 1/(1+e^(bar_dp)).
7. Define the variables for each horizon \(H\) as follows:
    future_return= sum_{h=1}^{H} kappa^{h-1}re_{t+12h}
    change_d = -sum_{h=1}^{H}\kappa^{h-1}dg_{t+12h}
    dp_H = kappa^H dp_{t+12H},
    where the dataset is monthly, so t+12h denotes the observation \(h\) years ahead of the current row.
8. The dataset contains monthly observations of annual variables. For every eligible monthly row \(m\), retain that row as a regression starting observation. For horizon \(H\) years, use annual observations at row indices \(m+12h\), for \(h=1,\ldots,H\), rather than summing every intervening monthly row. Construct future_return, change_d, and dp_H using the formulas above.
9. Run an OLS regression of future_return on dp (with intercept) using this H's cleaned data; store the slope coefficient in a list, e.g., b_re_list.
10. Run an OLS regression of change_d on dp (with intercept) using this H's cleaned data; store the slope coefficient in a list, e.g., b_dg_list.
11. Run an OLS regression of dp_H on dp (with intercept) using this H's cleaned data; store the slope coefficient in a list, e.g., b_dpH_list.
12. Plot the coefficients from the OLS regressions against the horizon H to visualize the relationship as three lines, one in red, one in blue, and one in green. With clear labels and a legend. in the plot, the x-axis should represent the horizon H from 1 to 20, and the y-axis should represent the coefficients from the OLS regressions.
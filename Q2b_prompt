Provide me with Python code following these steps:
1. Read the csv file "EQ_Dataset.csv" into a pandas DataFrame
2. Display the first few rows of the DataFrame to get an overview of the data.
3. Create a monthly date variable using "YEAR" and "MONTH", and sort the DataFrame chronologically.
4. Check for any missing values in the DataFrame to ensure data completeness.
5. Construct the variables
Let \tau index the monthly observations in the dataset.
Construct the simple annual excess equity return as
 xR_{e,\tau}=e^{re_\tau}-e^{rf_\tau}, 
so in Python use
xRe = np.exp(re) - np.exp(rf)
and construct the dividend-price ratio as
 DP_\tau=\frac{D_\tau}{P_\tau}=e^{dp_\tau}, 
so in Python use
DP = np.exp(dp)
6. Construct the predictive regression
The data are observed monthly, but the dependent variable is an annual return. Therefore, let the return horizon be H=12 months.
For each eligible monthly starting observation \tau, estimate the predictive regression
 xR_{e,\tau\rightarrow\tau+12} = a+bDP_\tau+\epsilon_\tau. 
Use the current dividend-price ratio DP_\tau as the predictor and the corresponding annual excess return over the 12-month horizon as the dependent variable.
Retain only observations for which both variables required for the regression are available. Do not replace missing observations with zeros.
Estimate the regression by OLS with an intercept.

7. Report the OLS estimate \hat_{b}. Then obtain the standard error and corresponding t-statistic for \hat_{b} using each of the following five methods:
(i) Baseline OLS standard errors
Use the conventional homoskedastic OLS covariance estimator.
(ii) White (1980) standard errors
Use heteroskedasticity-robust standard errors that do not correct for autocorrelation. Use the standard White/HC0 covariance estimator.
(iii) Newey-West (1987) standard errors with 11 lags
Use a HAC covariance estimator with exactly
 L=11 
lags and Bartlett/Newey-West weights
 w_\ell=1-\frac{\ell}{L+1}. 
The use of 11 lags reflects the overlapping annual-return observations in the monthly dataset.
(iv) Hansen-Hodrick (1980) standard errors with 11 lags
Implement the Hansen-Hodrick covariance estimator with
 L=11 
and equal weights
 w_\ell=1 
for all \ell=1,....,11.
Do not simply use the standard Newey-West/Bartlett HAC estimator for this case, because Hansen-Hodrick uses equal weights rather than Bartlett weights.
(v) Newey-West (1987, 1994) standard errors with a data-driven lag selection
Implement the Newey-West HAC estimator using the automatic/data-driven bandwidth selection described by Newey and West (1994), rather than manually fixing the number of lags. 
Do not assume the selected lag length in advance. Clearly state the bandwidth-selection formula used, show the quantities entering the calculation, report the resulting bandwidth and the rule used to convert it to an integer lag length if necessary, and report the final selected lag L. Do not silently rely on a software package's default bandwidth selection.

For each of the five standard-error methods, calculate the t-statistic as
 t_b=\frac{\hat_{b}}{SE(\hat_{b})}. 
Since all five methods use the same OLS coefficient estimate, \hat_{b} should be identical across the five methods; only the estimated standard error and therefore the t-statistic should differ.
Produce a table with one row for each standard-error method and the following columns. Report all numerical values to 4 decimal places:
Method
\hat_{b}
Standard Error of \hat_{b}
t-statistic of \hat_{b}
Number of lags/bandwidth used, where applicable

Please keep the Python code straightforward. Use pandas, numpy, statsmodels, and other standard Python packages if needed. Add brief comments explaining the important steps. 
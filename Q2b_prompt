Provide me with Python code following these steps:
1. Read the csv file "EQ_Dataset.csv" into a pandas DataFrame
2. Display the first few rows of the DataFrame to get an overview of the data.
3. Create a monthly date variable using "YEAR" and "MONTH", and sort the DataFrame chronologically.
4. Check for any missing values in the DataFrame to ensure data completeness.
5. Create the simple annual equity market return
R_{e,t}=e^{re_t},
and the simple annual risk-free return
R_{f,t}=e^{rf_t}. 
Then create the simple excess return
 xR_{e,t}=e^{re_t}-e^{rf_t}. 
In Python, define
xRe = np.exp(re) - np.exp(rf).
Create the dividend-price ratio
 D_t/P_t=e^{dp_t}. 
In Python, define
DP = np.exp(dp).
The dataset contains monthly observations of annual variables. For every eligible monthly row t, retain that row as a starting observation. Since t+1 means one year ahead, construct
 xR_{e,t+1}=xR_{e,t+12} 
in the monthly dataset. Thus, pair the current dividend-price ratio D_t/P_t with the excess return exactly 12 months ahead. Do not shift by one monthly row.
Estimate by OLS, including an intercept,
 xR_{e,t+12} = a+b(D_t/P_t)+\epsilon_t. 
The regressor must be the dividend-price ratio at the starting observation t, while the dependent variable is the annual excess return ending 12 months later.
Only retain observations for which both D_t/P_t and xR_{e,t+12} are available. Do not replace missing observations with zeros.
Report the OLS estimate \hat b. Then obtain the standard error and corresponding t-statistic for \hat b using each of the following five methods:
(i) Baseline OLS standard errors
Use the conventional homoskedastic OLS covariance estimator.
(ii) White (1980) standard errors
Use heteroskedasticity-robust standard errors that do not correct for autocorrelation. Use the standard White/HC0 covariance estimator.
(iii) Newey-West (1987) standard errors with 11 lags
Use a HAC covariance estimator with exactly
 L=11 
lags and Bartlett/Newey-West weights
 w_\ell=1-\frac{\ell}{L+1}. 
The use of 11 lags reflects the overlapping annual-return observations in the monthly dataset.
(iv) Hansen-Hodrick (1980) standard errors with 11 lags
Implement the Hansen-Hodrick covariance estimator with
 L=11 
and equal weights
 w_\ell=1 
for all \ell=1,\ldots,11.
Do not simply use the standard Newey-West/Bartlett HAC estimator for this case, because Hansen-Hodrick uses equal weights rather than Bartlett weights.
(v) Newey-West (1987, 1994) standard errors with a data-driven lag selection
Implement the Newey-West HAC estimator using the automatic/data-driven bandwidth selection described by Newey and West (1994), rather than manually fixing the number of lags at 11. Clearly report the selected lag/bandwidth used by the implementation.
For each of the five standard-error methods, calculate the t-statistic as
 t_b=\frac{\hat b}{SE(\hat b)}. 
Since all five methods use the same OLS coefficient estimate, \hat b should be identical across the five methods; only the estimated standard error and therefore the t-statistic should differ.
Produce a table with one row for each standard-error method and the following columns:
Method
\hat b
Standard Error of \hat b
t-statistic of \hat b
Number of lags/bandwidth used, where applicable
As a diagnostic, also print the number of regression observations and verify that the dependent variable is exactly the 12-month-ahead xRe, while the predictor is the current DP.
Please keep the Python code straightforward. Use pandas, numpy, statsmodels, and other standard Python packages if needed. Add brief comments explaining the important steps. 